{
  "name": "kkdz",
  "private": true,
  "scripts": {
    "start": "rimraf .parcel-cache .nyc_output coverage dist && parcel src/index.html",
    "test": "cypress open",
    "test:ci": "wait-on http://localhost:1234 && cypress run && nyc report --reporter=lcov --reporter=text-summary"
  },
  "devDependencies": {
    "@babel/core": "^7.18.10",
    "@cypress/code-coverage": "^3.10.0",
    "babel-plugin-istanbul": "^6.1.1",
    "cypress": "^10.4.0",
    "nyc": "^15.1.0",
    "parcel": "^2.6.2",
    "rimraf": "^3.0.2",
    "typescript": "^4.7.4",
    "wait-on": "^6.0.1"
  }
}
