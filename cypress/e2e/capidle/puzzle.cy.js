import { generatePuzzle } from "../../../src/capidle/script/puzzle";

describe("generatePuzzle", () => {
  it("generates a random puzzle when you call the function", () => {
    // TODO: this code hasn't been written yet so the test always returns the same result
    expect(generatePuzzle()).to.eq("Tenerife");
  });
});
